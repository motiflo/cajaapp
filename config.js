// NO subir este archivo: contiene la API key real
// Este archivo contiene un placeholder para evitar exponer la clave en el repo.
// Reemplaza localmente el valor en tu entorno de desarrollo (no subirlo).
window.API_KEY = "REPLACE_WITH_YOUR_API_KEY"; // PLACEHOLDER
